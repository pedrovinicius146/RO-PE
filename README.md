npm install express ejs mongoose body-parser

npm install --save-dev nodemon

EM package.json por:

"scripts": {
  "start": "node app.js",
  "dev": "nodemon app.js"
}

pra iniciar o projeto:
 
 npm run dev
